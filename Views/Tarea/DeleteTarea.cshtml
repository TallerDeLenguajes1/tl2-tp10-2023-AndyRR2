@{ 
    ViewData["Title"] = "Eliminar Tarea";
}

@model Tarea
<h2>Confirmar eliminación</h2>

<form asp-controller="Tarea" asp-action="DeleteFromForm">
    <input asp-for="@Model.Id" type="hidden" />
    <button type="submit" class="btn btn-danger">Eliminar</button>
</form>